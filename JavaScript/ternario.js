let isValid = true;

function verify(isValid){
    if(isValid){
        return true;
    }else{
        return false;
    }
}


const result = isValid ? true : false;

console.log(result);